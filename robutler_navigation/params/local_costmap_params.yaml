local_costmap:
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 5.0
  publish_frequency: 2.0
  #transform_tolerance: 0.3
  static_map: false
  rolling_window: true  # If the static_map parameter is set to true, this parameter must be set to false. 
  width: 5.0    #NEVER PUT THIS VALUE TO 0.0, a origin of sensor out of the map will apear
  height: 5.0
  resolution: 0.05
  inflation_layer: 
    inflation_radius: 0.01       # 0.0 Localdeki alanlar
 
